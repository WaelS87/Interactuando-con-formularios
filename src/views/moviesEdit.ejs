<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title class="titulo">Editar Película</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <figure>
        <img src="/img/logo-DH.png" alt="Logo Digital House">
      </figure>
    <h1 class="titulo">Película Editada:  <%= Movie.title %></h1>
    <section class="formulario">
        
            <form action="/movies/update/<%= Movie.id %>?_method=PUT" method="POST" id="form">
                <label for="title">Título </label>
                <p><input type="text" name="title" id="title" value="<%= Movie.title %>" required></p>
                <small id="msgTit"></small>
                <label for="rating">Calificación </label>
                <p><input type="number" name="rating" id="rating" value="<%= Movie.rating %>" required></p>
                <small id="msgRat"></small>
                <label for="awards">Premios </label>
                <p><input type="number" name="awards" id="awards" value="<%= Movie.awards %>" required></p>
                <small id="msgAwa"></small>
                <label for="release_date">Fecha de creación </label>
                <p><input type="date" name="release_date" id="release_date" value="<%= Movie.release_date %> required"></p>
                <small id="msgRel"></small>
                <label for="length">Duración </label>
                <p><input type="number" name="length" id="length" value="<%= Movie.length %>" required></p>
                <small id="msgLen"></small>
                <label for="genre_id">Género </label>
                <p>
                    <select name="genre_id" id="genre_id" required>
                        <% allGenres.forEach(genre => { %>
                        <option value="<%= genre.id %>" <% if (Movie.genre.id == genre.id) { %><%= 'selected' %><% }; %>><%= genre.name %></option>
                        <% }); %>
                    </select>
                    <small id="msgGen"></small>
                </p>

        <!--
                <label for="genre_id">Genre ID </label>
                <p><input type="number" name="genre_id" id="" value="<%= Movie.genre_id %>"></p>
            -->
                
                <br>
                <p> <button class="botonAgregar" type="submit" id="submit" disabled>Actualizar</button>
            
                <a  class="botonVolver" href="/movies">Listado de Películas</a><small id="msgSub"></small></p>
            </form>
        
    </section>
    <script src="/js/movies.js"></script>
</body>

</html>